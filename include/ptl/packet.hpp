#ifndef PACKET_HPP
#define PACKET_HPP

#include <ptl/packet.h>

namespace packet
{

typedef packet::c::packet_size_t size_t;
typedef packet::c::packet_header_t header_t;

} //namespace packet

#endif

